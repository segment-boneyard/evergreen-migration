machine:
  node:
    version: 8

dependencies:
  pre:
    - npm config set "//registry.npmjs.org/:_authToken" $NPM_AUTH
  cache_directories:
    - ~/.cache/yarn
  override:
    - yarn install

test:
  override:
    - yarn run test
